import { render,screen } from "@testing-library/react"; import { describe,expect,it } from "vitest"; import { SessionTimeline } from "./session-timeline"; import { Status } from "./status";
describe("status surfaces",()=>{it("renders live session state",()=>{render(<SessionTimeline sessions={[{status:"LIVE",session:{type:"R",name:"Race",date:"2026-09-10",start:"2026-09-10T10:00:00Z",source:"test",cancelled:false}}]}/>);expect(screen.getByText("LIVE")).toBeInTheDocument();expect(screen.getByText("RACE")).toBeInTheDocument()});it("renders freshness",()=>{render(<Status value="STALE"/>);expect(screen.getByText("STALE")).toHaveClass("status-stale")})});
