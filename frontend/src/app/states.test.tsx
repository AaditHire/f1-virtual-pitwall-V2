import { fireEvent,render,screen } from "@testing-library/react";import { describe,expect,it,vi } from "vitest";import Loading from "./loading";import ErrorPage from "./error";
describe("route states",()=>{it("exposes a loading state",()=>{render(<Loading/>);expect(screen.getByLabelText("Loading")).toBeInTheDocument()});it("shows and retries an error",()=>{const reset=vi.fn();render(<ErrorPage error={new Error("backend down")} reset={reset}/>);expect(screen.getByRole("alert")).toHaveTextContent("backend down");fireEvent.click(screen.getByRole("button",{name:"Try again"}));expect(reset).toHaveBeenCalled()})});
